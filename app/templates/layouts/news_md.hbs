{{> head ~}}

{{> header ~}}

<section class="news-archive section">
    <div class="container">
        <h1>{{page.title}}</h1>
        <div class="row">
            <div class="col-lg-12">

                {{#markdown}}
                    {{> body }}
                {{/markdown ~}}

                <p>
                    <i class="fa fa-home"></i>
                    <a href="/news/">Back to the news overview</a>
                </p>
            </div>
        </div>
    </div>
</section>

{{> footer ~}}

{{> scripts ~}}