{{> head ~}}

{{> header ~}}

    <section class="security section">
        <div class="container">
            <h1>{{page.title}}</h1>
            <div class="row">
                <div class="col-lg-12">

                    {{> body }}

                    <p>
                        <i class="fa fa-home"></i>
                        <a href="/advisories">Back to the security advisories overview</a>
                    </p>
                </div>
            </div>
        </div>
    </section>

{{> footer ~}}

{{> scripts ~}}